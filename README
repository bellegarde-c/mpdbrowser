Installation:
./setup.py install --prefix=/usr

Setting prefix will only work at first run (install issue)

Run app:
- Run ./mB to run from dir
- Run mpdBrowser after installation.

To use a custom empty cover:
~/.local/share/mpdBrowser/empty.png
